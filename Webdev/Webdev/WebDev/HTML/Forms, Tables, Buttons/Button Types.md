#Q
How can button be integrated with forms
?
You can nest it inside `<form>` element to make button to submit the button and all

#Q
What is the element for button called
?
`<input>` and `<button>`

#Q #AOSR/z6000
What are the types of buttons
? #AOSR/z6000/m/olnd
`submit`
`reset`
`button`

#Q
What is the difference between Input and Button element for input method
?
`<button>` is an opening and closing container tag that can hold HTML content (like text, images, or icons), while `<input>` is a self-closing empty element whose display value is limited to plain text specified via attributes.


#Q
How to show button text for input button type
?
Using `value` attribute
